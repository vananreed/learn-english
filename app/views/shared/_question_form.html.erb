<%= simple_form_for @question, method: :post, url: questions_path do |f| %>
  <%= f.input :name, label: "Your Name" %>
  <%= f.input :email %>
  <%= f.input :content, label: "Question" %>
  <%= f.submit "Send Email", class: "btn btn-primary" %>
<% end %>
